var config = {};

config.messages = {
  success: 'Success. Nothing went wrong.',
  error: 'Sorry , Something went wrong. Please try again.',
  fieldMissing: 'Sorry, Required fields are missing.',
  userAlreadyExists: 'Sorry, This user already exists.',
};

module.exports = config;
